---
"dkcutter": major
---

feat!: remove deprecated `--init` flag

The `--init` CLI flag was deprecated in v6 and has been completely removed in v7.
To initialize a base DKCutter template, please use the dedicated `init` command instead:

```bash
dkcutter init
```
