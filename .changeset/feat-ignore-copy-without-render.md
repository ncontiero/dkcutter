---
"dkcutter": minor
---

feat: add `ignore` and `copyWithoutRender` to config

- Introduces two new properties in `_dkcutter` config to ignore files or copy them without rendering, utilizing `tinyglobby` for pattern matching.
